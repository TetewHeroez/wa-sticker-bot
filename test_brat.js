const { createCanvas } = require('@napi-rs/canvas'); const fs = require('fs'); const CANVAS_SIZE = 512; const PADDING = 40; function wrapText(ctx, text, maxWidth) { const lines = []; const paragraphs = text.split('\n'); for (const p of paragraphs) { let currentLine = ''; const words = p.split(/\s+/).flatMap(w => { if (w.includes('-') && w !== '-') { const parts = w.split('-'); const res = []; for (let i = 0; i < parts.length - 1; i++) res.push(parts[i] + '-'); res.push(parts[parts.length - 1]); return res.filter(x => x); } return w; }); for (const word of words) { const needsSpace = currentLine.length > 0 && !currentLine.endsWith('-'); const testLine = currentLine ? (needsSpace ? currentLine + ' ' + word : currentLine + word) : word; if (ctx.measureText(testLine).width > maxWidth && currentLine) { lines.push(currentLine); currentLine = word; } else { currentLine = testLine; } while (ctx.measureText(currentLine).width > maxWidth) { let temp = ''; let remainder = currentLine; let broken = false; for (let i = 0; i < remainder.length; i++) { if (ctx.measureText(temp + remainder[i]).width > maxWidth) { if (i === 0) { temp = remainder[0]; currentLine = remainder.slice(1); } else { lines.push(temp); currentLine = remainder.slice(i); } broken = true; break; } temp += remainder[i]; } if (!broken) break; } } if (currentLine) lines.push(currentLine); } return lines; } const canvas = createCanvas(CANVAS_SIZE, CANVAS_SIZE); const ctx = canvas.getContext('2d'); ctx.fillStyle = '#FFFFFF'; ctx.fillRect(0, 0, CANVAS_SIZE, CANVAS_SIZE); const text = 'hei antek-antek aseng'; ctx.font = '80px Arial'; const lines = wrapText(ctx, text, CANVAS_SIZE - PADDING * 2); ctx.fillStyle = 'black'; ctx.textAlign = 'left'; ctx.textBaseline = 'middle'; ctx.filter = 'blur(1px)'; ctx.save(); ctx.scale(0.75, 1.2); lines.forEach((l, i) => { ctx.fillText(l, PADDING / 0.75, (100 + i * 80 * 1.2) / 1.2); }); ctx.restore(); fs.writeFileSync('test_out.png', canvas.toBuffer('image/png')); console.log('Done, check test_out.png');
